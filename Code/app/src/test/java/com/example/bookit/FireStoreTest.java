package com.example.bookit;

import com.google.firebase.FirebaseApp;

import org.junit.jupiter.api.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

public class FireStoreTest {
    /*
    @Test
    public void testLoginAuth() {
        FireStoreHelper db=new FireStoreHelper(null);
        boolean a=db.loginAuth("xiu@ualberta.ca","1234567",null);//try with the test account
        assertTrue(a);
        boolean b=db.loginAuth("xiu@ualberta.ca","12345678",null);//try with the test account
        assertFalse(b);

    }*/
}

